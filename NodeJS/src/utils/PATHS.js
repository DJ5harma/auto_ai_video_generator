import path from "path";

export const PATHS = {
    audioPath : path.join(process.cwd(), "data", "audio.wav"),
    tempDir : path.join(process.cwd(), "data", "temp_frames"),
    imageDir : path.join(process.cwd(), "data", "images"),
}